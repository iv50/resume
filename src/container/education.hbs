<div>
     {{#with main}}             
        <main>
            <section>
                {{> title data="My education" big=true }}      
                <ul>
                    {{#each educations}}
                    <li>
                        {{name}}
                        {{#unless isEnd}}
                        (ще в процесі навчання) 
                        {{/unless}}
                        {{!-- {{#each educations}}
                        <li>{{this}}</li>
                        {{/each}} --}}
                    </li>
                    {{/each}}
                    
                </ul>
            </section>
            <section>
                {{> title data="My sertificates"}}
                <ul>
                    {{#each certificates}}
                    <li>
                        {{name}} ({{id}})
                    </li>
                    {{/each}}
                </ul>
            </section>
        </main>
    {{/with}} 
</div>
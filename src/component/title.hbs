{{!-- 
    <div>
        <h2 >{{data}} </h2> 
    </div> --}}

{{#if big}}
    <h2 >{{data}} </h2> 
{{else}}
    <h3 >{{data}} </h3>
{{/if}} 

 


 {{!-- git push origin person
  git commit -m "sd"
  git status
  git checkout -b person
   git push origin master
   git add .
   git pull origin master
   git checkout master
   git remote add origin https://github.com/iv50/resume.git
   git npm rum dev --}}